services:

  sql-server:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sql-server-container
    user: root
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "SqlServer2026"
    ports:
      - "1433:1433"
    volumes:
      - sql-data:/var/opt/mssql
    restart: unless-stopped
    networks:
      - sql-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'SqlServer2026' -C -Q 'SELECT 1' || exit 1"
        ]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  db-init:
    image: mcr.microsoft.com/mssql/server:2022-latest
    depends_on:
      sql-server:
        condition: service_healthy
    networks:
      - sql-network
    restart: "no"
    command: >
      /bin/bash -c "
      /opt/mssql-tools18/bin/sqlcmd
      -S sql-server
      -U sa
      -P 'SqlServer2026'
      -C
      -Q \"IF DB_ID('SupermarketDB') IS NULL CREATE DATABASE SupermarketDB\"
      "
  liquibase:
    image: liquibase/liquibase:5.0.1
    container_name: liquibase-container

    depends_on:
      db-init:
        condition: service_completed_successfully

    volumes:
      - ./backend/src/main/resources/liquibase:/workspace/
      - ./backend/src/main/resources/liquibase/drivers/mssql-jdbc-13.4.0.jre11.jar:/liquibase/lib/mssql-jdbc.jar

    command: >
      --defaultsFile=/workspace/liquibase.properties
      --searchPath=/workspace
      update

    networks:
      - sql-network
    restart: "no"

  backend:
    build: ./backend
    container_name: backend-BJJMarket
    depends_on:
      liquibase:
        condition: service_completed_successfully
    env_file:
      - ./backend/src/main/resources/.env
    ports:
      - "9090:9090"
    restart: unless-stopped
    networks:
      - sql-network

  frontend-web:
    container_name: frontend-web-BJJMarket
    build:
      context: ./frontend-web
      dockerfile: dev.Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend-web:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: "true"

networks:
  sql-network:
    driver: bridge

volumes:
  sql-data:
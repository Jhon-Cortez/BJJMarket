services:

  postgres:
    image: postgres:16-alpine
    container_name: postgres-bjjmarket
    environment:
      POSTGRES_DB: bjjmarket
      POSTGRES_USER: bjjmarket
      POSTGRES_PASSWORD: BjjMarket2026!
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    restart: unless-stopped
    networks:
      - db-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bjjmarket -d bjjmarket"]
      interval: 10s
      timeout: 5s
      retries: 10
  liquibase:
    build:
      context: ./docker/liquibase
    image: bjjmarket-liquibase:5.0.1
    container_name: liquibase-container

    depends_on:
      postgres:
        condition: service_healthy

    volumes:
      - ./backend/src/main/resources/liquibase:/workspace/

    command: >
      --url=jdbc:postgresql://postgres:5432/bjjmarket
      --username=bjjmarket
      --password=BjjMarket2026!
      --driver=org.postgresql.Driver
      --changelog-file=master.xml
      --searchPath=/workspace
      update

    networks:
      - db-network
    restart: "no"

  backend:
    build: ./backend
    container_name: backend-BJJMarket
    depends_on:
      liquibase:
        condition: service_completed_successfully
    env_file:
      - ./backend/src/main/resources/.env
    ports:
      - "9090:9090"
    restart: unless-stopped
    networks:
      - db-network

  frontend-web:
    container_name: frontend-web-BJJMarket
    build:
      context: ./frontend-web
      dockerfile: dev.Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend-web:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: "true"
      VITE_API_URL: "http://10.3.232.162:9090/api"
      # Vite admite este nombre de host temporal mediante su variable oficial.
      __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS: ${VITE_ALLOWED_HOSTS:-}

networks:
  db-network:
    driver: bridge

volumes:
  postgres-data:
